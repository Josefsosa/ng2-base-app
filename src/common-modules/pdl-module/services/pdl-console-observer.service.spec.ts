import { PdlConsoleObserver } from './pdl-console-observer.service';
import { expect } from 'chai';
require('sinon');

describe('PDL Console Observer test suite', () => {
  it('Test PDL Console Observer', () => {
    expect(true).not.to.be.null;
    expect(true).not.to.be.undefined;
    expect(true).not.to.be.empty;
  });
});