import { PdlDirectiveDecorator } from './pdl.directive.decorator';
import { expect } from 'chai';
require('sinon');

describe('PDL Directive Decorator test suite', () => {
  it('Test PDL Directive Decorator', () => {
    expect(true).not.to.be.null;
    expect(true).not.to.be.undefined;
    expect(true).not.to.be.empty;
  });
});