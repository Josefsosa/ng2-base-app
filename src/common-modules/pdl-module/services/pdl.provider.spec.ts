import { PdlProvider } from './pdl.provider';
import { expect } from 'chai';
require('sinon');

describe('PDL Provider test suite', () => {
  it('Test PDL Provider', () => {
    expect(true).not.to.be.null;
    expect(true).not.to.be.undefined;
    expect(true).not.to.be.empty;
  });
});